/** Small inline loading spinner, sized to swap in place of a button's icon without changing the button's width. */
export function Spinner() {
  return <span className="spinner" role="status" aria-label="Loading" />;
}
